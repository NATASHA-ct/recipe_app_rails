<p style="color: green"><%= notice %></p>

<h1>Recipefoods</h1>

<div id="recipefoods">
  <% @recipe.recipefoods.each do |recipefood| %>
    <%= render recipefood %>
    <p>
      <%= link_to "Show this recipefood", recipe_recipefoods_path(@recipe) %>
    </p>
  <% end %>
</div>

<%= link_to "New recipefood", new_recipefood_path %>
